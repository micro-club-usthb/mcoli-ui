---
title: MC Input
description: A text field for user input.
---

## Overview

The `mc-input` is a versatile component of the MicroClub UI library. Built on the **@base-ui/react** input primitive, it delivers elite accessibility and tactile interaction, wrapped in MicroClub's unique **Tailwind CSS v4** design tokens.

Deploy this component instantly into your project to achieve professional, accessible forms without the bulk of a global dependency.

## Preview

<Tabs items={['preview', 'code']}>
  <Tab>
    <ComponentPreview name='mc-input-demo'/>
  </Tab>
  <Tab>
    <ComponentSource name='mc-input-demo' />
  </Tab>
</Tabs>

## Add to Your Project

Deploy the `mc-input` directly to your `components/ui` directory:

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab>
    ```sh
npx mcoli-ui@latest add mc-input
    ```
  </Tab>
</Tabs>

### Manual Setup

If you prefer manual control:

<Steps>

<Step>Install the core dependencies</Step>

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab>
    ```sh
npm install clsx tailwind-merge @base-ui/react class-variance-authority lucide-react
    ```
  </Tab>
</Tabs>

<Step>Copy the source code</Step>

<ComponentSource name='mc-input' />

<Step>Adjust import paths</Step>

Ensure your `@/lib/utils` or equivalent classname helper is correctly imported.

</Steps>

## Usage

```tsx
import { McInput } from "@/components/ui/mc-input"
import { Mail } from "lucide-react"

export default function Example() {
  return (
    <McInput 
      placeholder="Enter your email" 
      leftIcon={<Mail className="size-4" />}
    />
  )
}
```

## Examples

### Variants

```tsx
<McInput variant="default" placeholder="Default variant" />
<McInput variant="filled" placeholder="Filled variant" />
<McInput variant="outline" placeholder="Outline variant" />
```

### With Icons

```tsx
import { Search, Mail } from "lucide-react"

<McInput 
  leftIcon={<Search className="size-4" />} 
  placeholder="Search components..." 
/>

<McInput 
  rightIcon={<Mail className="size-4" />} 
  placeholder="Contact support" 
/>
```

### Validation States

The component supports standard HTML validation attributes and ARIA states.

```tsx
<McInput aria-invalid="true" placeholder="Invalid input" />
<McInput required placeholder="Required field" />
<McInput disabled placeholder="Disabled field" />
```

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "filled" \| "outline"` | `"default"` | The visual style of the input |
| `leftIcon` | `ReactNode` | `undefined` | Icon element to render inside the input on the left |
| `rightIcon` | `ReactNode` | `undefined` | Icon element to render inside the input on the right |
| `className` | `string` | `undefined` | Additional CSS classes for the input wrapper |

All other props from `@base-ui/react/input` are also supported.
