{
  "name": "mcoli-ui",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @mcoli/ui dev",
    "build": "pnpm --filter @mcoli/ui build",
    "start": "pnpm --filter @mcoli/ui start",
    "lint": "pnpm --filter @mcoli/ui lint",
    "typecheck": "pnpm --filter @mcoli/ui typecheck",
    "storybook": "pnpm --filter @mcoli/ui storybook",
    "build-storybook": "pnpm --filter @mcoli/ui build-storybook",
    "cli": "pnpm --filter mcoli-ui",
    "cli:dev": "MCOLI_UI_REGISTRY_URL=http://localhost:3000 pnpm --filter mcoli-ui",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,md,mdx,json}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^9",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "typescript": "^5.7.2"
  }
}
